interface SiteCardProps {
    id: string
    name: string
    author: string
    description: string
    imageUrl?: string
}

interface WalursLogObject {
    suiObj: string,
    name: string,
    desc: string,
}